$scope.classSelected = function() {
var class = characterService.characterCreationObject.static.classes[0].class

	for (var i = 0; i < $scope.classes; 1++) {
		if ($scope.classes[i].class === class) {
		return
		}
	}
$scope.charHitPoints = function() {
	for (var i = 0; i <
}
